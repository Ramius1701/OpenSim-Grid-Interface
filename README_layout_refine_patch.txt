Casperia Prime – Layout Refine Patch

What this patch does:
- Adjusts main-container padding to match Bootstrap gutter math so hero sections and grid (.row/.col) align.
- Reduces 'wide' max-width to 1320px (Bootstrap XXL) so pages are wider but not overly wide.
- Adds a central default list of pages that should use 'wide' layout automatically (economy, gridstatus, gridmap, gridsearch, events, classifieds, destinations).

Install:
- Unzip over your existing Casperia site, allowing overwrite of:
  casperia/include/header.php
